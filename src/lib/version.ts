/**
 * Shown quietly at the foot of the home screen so the product owner can tell
 * at a glance which build their phone is actually running — indispensable
 * when a cached page and a fresh deploy look identical, and more so once the
 * Phase 7 service worker adds another cache layer. Moves into the Settings
 * screen when that exists.
 */
export const APP_VERSION = 'Phase 6'
